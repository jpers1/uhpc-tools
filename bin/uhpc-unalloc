#!/usr/bin/env bash
#
# uhpc-unalloc: Terminate/cancel a job (and all shells).
#
# Usage: uhpc-unalloc <JOBID>
#
# Example:
#   uhpc-unalloc 123456

JOBID="$1"

if [ -z "$JOBID" ]; then
  echo "Usage: $0 <JOBID>"
  exit 1
fi

echo "[uhpc-unalloc] Canceling job $JOBID ..."
scancel "$JOBID"
